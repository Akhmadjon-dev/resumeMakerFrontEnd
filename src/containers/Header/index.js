import React, { Component } from 'react'
import StyledHeader from '../../style/StyHeader';

export default class Header extends Component {
    render() {
        return (
            <StyledHeader>
                header
            </StyledHeader>
        )
    }
}
